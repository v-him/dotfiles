# vim: set ft=zsh:

emulate -RL zsh

local output
local -i exit_status

output=$(eval $1 2>&1)
exit_status=$?

if (( exit_status != 0 )); then
	print -- $output >&2
	return $exit_status
fi

bat --style=plain --language=help <<<$output
