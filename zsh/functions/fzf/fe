# fe [FUZZY PATTERN] - Open the selected file(s) with the default editor

emulate -RL zsh

fzf --multi --exit-0 \
	--bind "enter:become(${EDITOR:-vim} {+})" \
	--walker-root ${*:-.}

# vim: set ft=zsh:
